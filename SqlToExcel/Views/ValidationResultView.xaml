<Window x:Class="SqlToExcel.Views.ValidationResultView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlToExcel.Views"
        mc:Ignorable="d"
        Title="行验证结果" Height="450" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Summary}" FontWeight="Bold" Margin="5"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="5">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMatch}" Value="False">
                            <Setter Property="Background" Value="#FFCDD2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="字段名" Binding="{Binding ColumnName}" Width="*"/>
                <DataGridTextColumn Header="源数据" Binding="{Binding SourceValue}" Width="*"/>
                <DataGridTextColumn Header="目标数据" Binding="{Binding TargetValue}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
