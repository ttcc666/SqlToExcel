<UserControl x:Class="SqlToExcel.Views.TableComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SqlToExcel.Views"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <hc:Card Header="JSON源表" Grid.Row="0">
            <TextBox Text="{Binding JsonSourceTables, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" 
                     Height="100" 
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas"/>
        </hc:Card>

        <hc:Card Header="比对结果 (目标数据库中存在但JSON源中没有的表)" Grid.Row="1" Margin="0,10,0,0">
            <DataGrid ItemsSource="{Binding MissingTablesResult}" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="表名" Binding="{Binding}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </hc:Card>

        <Button Grid.Row="2" 
                Content="开始比对并保存结果"
                Command="{Binding CompareCommand}" 
                Style="{StaticResource ButtonPrimary}" 
                HorizontalAlignment="Right" 
                Margin="0,10,0,0"/>
    </Grid>
</UserControl>
