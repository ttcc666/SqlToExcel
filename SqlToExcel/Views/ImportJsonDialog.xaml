<hc:BlurWindow x:Class="SqlToExcel.Views.ImportJsonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlToExcel.Views"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="导入JSON配置" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="在此处粘贴JSON内容，或从文件加载。" Margin="0,0,0,10"/>

        <hc:TextBox Grid.Row="1" 
                    Text="{Binding JsonText, UpdateSourceTrigger=PropertyChanged}"
                    AcceptsReturn="True" 
                    VerticalScrollBarVisibility="Auto"
                    FontFamily="Consolas"
                    TextWrapping="Wrap"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="生成模板" Command="{Binding GenerateTemplateCommand}" Margin="0,0,10,0"/>
            <Button Content="从文件加载" Command="{Binding LoadFromFileCommand}" Margin="0,0,10,0"/>
            <Button Content="导入" Command="{Binding ImportCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" IsDefault="True" Style="{StaticResource ButtonPrimary}"/>
            <Button Content="取消" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" IsCancel="True" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</hc:BlurWindow>