<Window x:Class="SqlToExcel.Views.FieldComparisonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="字段对比工具 (源数据库)" 
        Height="650" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        Icon="/Icon/icon.ico">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200" MinHeight="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="字段对比工具" FontSize="20" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="输入JSON，与源数据库的表进行字段对比。数据库中存在但JSON中缺失的字段将以红色高亮显示。" Foreground="Gray" TextWrapping="Wrap"/>
        </StackPanel>

        <hc:Card Grid.Row="1" Header="JSON 输入 (格式: {&quot;table&quot;: &quot;表名&quot;, &quot;fields&quot;: [...]})" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Text="{Binding JsonInput, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         FontFamily="Consolas"
                         FontSize="12"
                         Padding="5"
                         BorderThickness="0"/>
            </ScrollViewer>
        </hc:Card>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,15">
            <Button Content="开始对比" 
                    Command="{Binding CompareFieldsCommand}"
                    IsEnabled="{Binding IsProcessing, Converter={StaticResource BoolToZeroOneConverter}}"
                    Style="{StaticResource ButtonPrimary}"
                    hc:IconElement.Geometry="{StaticResource App.EyeCheck}"
                    Width="140" 
                    Height="35"/>
            <Button Content="清空" 
                    Command="{Binding ClearCommand}"
                    Style="{StaticResource ButtonDefault}"
                    Margin="10,0,0,0"
                    Width="100" 
                    Height="35"/>
            <Button Content="导出到Excel" 
                    Command="{Binding ExportCommand}"
                    Style="{StaticResource ButtonSuccess}"
                    hc:IconElement.Geometry="{StaticResource App.MicrosoftExcel}"
                    Margin="10,0,0,0"
                    Width="140" 
                    Height="35"/>
            <ProgressBar IsIndeterminate="True" 
                         Width="120" 
                         Height="20" 
                         Margin="20,0,0,0"
                         Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <TabControl Grid.Row="3" ItemsSource="{Binding TabResults}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding TableName}" VerticalAlignment="Center"/>
                        <Ellipse Width="8" Height="8" Margin="8,0,0,0" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#28A745"/>
                                    <Setter Property="ToolTip" Value="处理成功"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding StatusMessage}" Value="处理失败">
                                            <Setter Property="Fill" Value="#DC3545"/>
                                            <Setter Property="ToolTip" Value="{Binding StatusMessage}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DataGrid ItemsSource="{Binding ComparisonResults}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="None"
                                  RowHeaderWidth="0"
                                  BorderThickness="0">
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="10, 8"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInJson}" Value="False">
                                            <Setter Property="Background" Value="#FFCDD2"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E8F5FF"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="数据库字段名" Binding="{Binding FieldName}" Width="*"/>
                                <DataGridTemplateColumn Header="JSON 状态" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="Bold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsInJson}" Value="True">
                                                                <Setter Property="Text" Value="✓"/>
                                                                <Setter Property="Foreground" Value="#28A745"/>
                                                                <Setter Property="ToolTip" Value="存在于JSON中"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInJson}" Value="False">
                                                                <Setter Property="Text" Value="✗"/>
                                                                <Setter Property="Foreground" Value="#DC3545"/>
                                                                <Setter Property="ToolTip" Value="在JSON中缺失"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Margin="5" FontStyle="Italic" Foreground="Gray"/>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <StatusBar Grid.Row="4" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>