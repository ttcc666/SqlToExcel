<Window x:Class="SqlToExcel.Views.ColumnSortView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlToExcel.Views"
        xmlns:viewModels="clr-namespace:SqlToExcel.ViewModels"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="Define Sort Order" Height="450" Width="600"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <Window.Resources>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <hc:Card Header="Available Columns" Grid.Column="0">
            <ListBox ItemsSource="{Binding AvailableColumns}" SelectedItem="{Binding SelectedAvailableColumn}" DisplayMemberPath="ColumnName" />
        </hc:Card>

        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Button Content=">" Command="{Binding AddColumnCommand}" Margin="5" />
            <Button Content="&lt;" Command="{Binding RemoveColumnCommand}" Margin="5" />
        </StackPanel>

        <hc:Card Header="Sort Order" Grid.Column="2">
            <ListBox ItemsSource="{Binding SortColumns}" SelectedItem="{Binding SelectedSortColumn}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding ColumnName}" VerticalAlignment="Center" />
                            <ComboBox Grid.Column="1" ItemsSource="{Binding DataContext.SortDirections, RelativeSource={RelativeSource AncestorType=Window}}" SelectedItem="{Binding Direction}" Margin="5,0" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </hc:Card>

        <StackPanel Grid.Column="3" VerticalAlignment="Center">
            <Button Content="↑" Command="{Binding MoveUpCommand}" Margin="2" />
            <Button Content="↓" Command="{Binding MoveDownCommand}" Margin="2" />
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Command="{Binding OkCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" IsDefault="True" MinWidth="60" Margin="5" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" IsCancel="True" MinWidth="60" Margin="5" />
        </StackPanel>
    </Grid>
</Window>