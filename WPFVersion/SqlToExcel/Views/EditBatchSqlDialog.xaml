<Window x:Class="SqlToExcel.Views.EditBatchSqlDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="编辑SQL与描述" Height="700" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="配置键：" FontWeight="Bold"/>
            <TextBlock Text="{Binding Key}"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="源 (Source)" Margin="0,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <hc:SimpleText Grid.Row="0" Text="描述"/>
                <TextBox Grid.Row="1" Text="{Binding SourceDescription, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" MaxLength="2000" Margin="0,2,0,5"/>
                <hc:SimpleText Grid.Row="2" Text="SQL"/>
                <TextBox Grid.Row="3" Text="{Binding SourceSql, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Margin="0,2,0,0"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="目标 (Target)" Margin="0,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,2,0,5">
                    <TextBlock Text="目标类型:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <RadioButton GroupName="Destination" Content="目标数据库" VerticalAlignment="Center" Margin="0,0,10,0">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource RadioButtonBaseStyle}">
                                <Setter Property="IsChecked" Value="False" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Destination}" Value="Target">
                                        <Setter Property="IsChecked" Value="True" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>
                    <RadioButton GroupName="Destination" Content="框架库" VerticalAlignment="Center">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton" BasedOn="{StaticResource RadioButtonBaseStyle}">
                                <Setter Property="IsChecked" Value="False" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Destination}" Value="Framework">
                                        <Setter Property="IsChecked" Value="True" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>
                </StackPanel>
                <hc:SimpleText Grid.Row="1" Text="描述"/>
                <TextBox Grid.Row="2" Text="{Binding TargetDescription, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" MaxLength="2000" Margin="0,2,0,5"/>
                <hc:SimpleText Grid.Row="3" Text="SQL"/>
                <TextBox Grid.Row="4" Text="{Binding TargetSql, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Margin="0,2,0,0"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="保存" Click="SaveButton_Click" Style="{StaticResource ButtonPrimary}" Margin="0,0,10,0"/>
            <Button Content="取消" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
