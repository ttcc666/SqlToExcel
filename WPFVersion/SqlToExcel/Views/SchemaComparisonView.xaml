<UserControl x:Class="SqlToExcel.Views.SchemaComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SqlToExcel.Views"
             xmlns:vm="clr-namespace:SqlToExcel.ViewModels"
             xmlns:converters="clr-namespace:SqlToExcel.Converters"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,5">
            <Button Content="导出" Command="{Binding ExportCommand}" Style="{StaticResource ButtonPrimary}"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{StaticResource ButtonInfo}" Margin="10,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ComparisonResults}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="10,0,10,10"
                  RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTextColumn Header="源表名" Binding="{Binding SourceTableName}" Width="*"/>
                <DataGridTextColumn Header="目标表名" Binding="{Binding TargetTableName}" Width="*"/>

                <DataGridTemplateColumn Header="源表主键" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding SourcePrimaryKeys}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <TextBlock.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="{Binding SourcePrimaryKeys}" />
                                    </ToolTip>
                                </TextBlock.ToolTip>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="目标表主键" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TargetPrimaryKeys}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis">
                                <TextBlock.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="{Binding TargetPrimaryKeys}" />
                                    </ToolTip>
                                </TextBlock.ToolTip>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Margin="10" Padding="10" BorderBrush="LightGray" BorderThickness="1" CornerRadius="3">
                        <TabControl>
                            <TabItem Header="源表索引">
                                <DataGrid ItemsSource="{Binding SourceIndexes}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="5">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="索引字段" Binding="{Binding ColumnsDisplay}"/>
                                        <DataGridTextColumn Header="索引名称" Binding="{Binding IndexName}"/>
                                        <DataGridCheckBoxColumn Header="主键" Binding="{Binding IsPrimaryKey}"/>
                                        <DataGridCheckBoxColumn Header="唯一" Binding="{Binding IsUnique}"/>
                                        <DataGridCheckBoxColumn Header="聚集" Binding="{Binding IsClustered}"/>
                                        <DataGridCheckBoxColumn Header="非聚集" Binding="{Binding IsNonClustered}"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </TabItem>
                            <TabItem Header="目标表索引">
                                <DataGrid ItemsSource="{Binding TargetIndexes}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="5">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="索引字段" Binding="{Binding ColumnsDisplay}"/>
                                        <DataGridTextColumn Header="索引名称" Binding="{Binding IndexName}"/>
                                        <DataGridCheckBoxColumn Header="主键" Binding="{Binding IsPrimaryKey}"/>
                                        <DataGridCheckBoxColumn Header="唯一" Binding="{Binding IsUnique}"/>
                                        <DataGridCheckBoxColumn Header="聚集" Binding="{Binding IsClustered}"/>
                                        <DataGridCheckBoxColumn Header="非聚集" Binding="{Binding IsNonClustered}"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </TabItem>
                        </TabControl>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>

        <ProgressBar Grid.RowSpan="2" IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="5"/>
    </Grid>
</UserControl>
