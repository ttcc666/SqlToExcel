<Window x:Class="SqlToExcel.Views.DatabaseConfigView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SqlToExcel.Views"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="数据库连接配置" Height="280" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="源数据库(Source):" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
        <hc:TextBox Text="{Binding SourceConnectionString, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5" />
        <Button Content="测试连接" Command="{Binding TestSourceConnectionCommand}" Grid.Row="0" Grid.Column="2" Margin="5,0,0,0" />

        <TextBlock Text="目标数据库(Target):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
        <hc:TextBox Text="{Binding TargetConnectionString, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5" />
        <Button Content="测试连接" Command="{Binding TestTargetConnectionCommand}" Grid.Row="1" Grid.Column="2" Margin="5,0,0,0" />

        <TextBlock Text="框架库(Framework):" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" />
        <hc:TextBox Text="{Binding FrameworkConnectionString, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="5" />
        <Button Content="测试连接" Command="{Binding TestFrameworkConnectionCommand}" Grid.Row="2" Grid.Column="2" Margin="5,0,0,0" />

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="保存" Command="{Binding SaveCommand}" IsDefault="True" Style="{StaticResource ButtonPrimary}" Margin="0,0,10,0" />
            <Button Content="取消" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>